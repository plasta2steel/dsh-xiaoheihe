# heybox bundle patch: one loader row for the plugin.
# The row `name` is this package itself, so the web modules scanner discovers
# the browser half through the `dsh.client` declaration in package.json.
- insert:
    - id: heybox
      name: heybox
